import LeanOrigami.Tactic.Elab

/-!
# Examples for the text-based origami tactic

During active tactic development, examples live at the bottom of
`LeanOrigami.Tactic.Elab` so one file session contains the implementation and
its smoke tests.  We can move them back here once the interface settles.
-/
